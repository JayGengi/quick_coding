<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 实心 -->
    <solid android:color="@color/color_white" />
    <!-- 边框 -->
    <stroke
        android:width="1dp"
        android:color="@color/common_color_line" />
    <!-- 圆角 -->
    <corners android:radius="6dp" />
    <!-- 边距 -->
    <padding
        android:bottom="0dp"
        android:left="0dp"
        android:right="0dp"
        android:top="0dp" />

</shape>