<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.guoxun.airbaba">

    <!-- 获取GSM网络信息状态，如当前的网络连接是否有效 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 允许访问Wi-Fi网络状态信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 允许获取Wi-Fi网络状态改变信息 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- 访问网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 读取手机状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_LOGS"
        tools:ignore="ProtectedPermissions" />
    <!-- SD 卡读写权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <meta-data
            android:name="design_width_in_dp"
            android:value="360"/>
        <meta-data
            android:name="design_height_in_dp"
            android:value="640"/>

        <activity
            android:name=".ui.activity.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 主页面 -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize"
            />
        <!-- 搜索頁 -->
        <activity
            android:name=".ui.activity.SearchActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />
        <!-- WebView -->
        <activity
            android:name=".ui.activity.WebViewActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 首页菜单二级首页 -->
        <activity
            android:name=".ui.activity.home.HomeMenuMainActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 厂家直销 -->
        <activity
            android:name=".ui.activity.home.HomeFactoryOutletActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 免费设计 -->
        <activity
            android:name=".ui.activity.home.HomeFreeDesignActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 消息中心 -->
        <activity
            android:name=".ui.activity.home.HomeMessageActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 订单消息 -->
        <activity
            android:name=".ui.activity.home.MessageActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 报修消息 -->
        <activity
            android:name=".ui.activity.home.RepairMessageActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 我的资产 -->
        <activity
            android:name=".ui.activity.home.AssetMessageActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 系统公告 -->
        <activity
            android:name=".ui.activity.home.SystemNoticeActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 精选活动 -->
        <activity
            android:name=".ui.activity.home.ActivityMessageActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 优惠活动 -->
        <activity
            android:name=".ui.activity.home.DiscountsActActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 商品分类 -->
        <activity
            android:name=".ui.activity.home.goods.GoodsTypeActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:screenOrientation="portrait" />

        <!-- 商品列表 -->
        <activity
            android:name=".ui.activity.home.goods.GoodsListActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:screenOrientation="portrait" />

        <!-- 商品列表 -->
        <activity
            android:name=".ui.activity.home.goods.GoodsDetailsActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 商家 -->
        <activity
            android:name=".ui.activity.home.goods.MerchantMainActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 搜索页 -->
        <activity
            android:name=".ui.activity.home.SearchActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 热销商品 -->
        <activity
            android:name=".ui.activity.home.goods.GoodsSellHotActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 设置 -->
        <activity
            android:name=".ui.activity.mine.setting.SettingActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 个人资料 -->
        <activity
            android:name=".ui.activity.mine.setting.PersonalDataActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 昵称 -->
        <activity
            android:name=".ui.activity.mine.setting.ModifyInfoActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 账户与安全 -->
        <activity
            android:name=".ui.activity.mine.account.AccountActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 钱包 -->
        <activity
            android:name=".ui.activity.mine.WalletActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 积分 -->
        <activity
            android:name=".ui.activity.mine.IntegralActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 修改新手机号 -->
        <activity
            android:name=".ui.activity.mine.account.ModifyPhoneActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 实名认证 -->
        <activity
            android:name=".ui.activity.mine.account.RealNameActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 绑定银行卡 -->
        <activity
            android:name=".ui.activity.mine.account.BankCardActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 用户协议 -->
        <activity
            android:name=".ui.activity.mine.account.UserAgreementActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 用户隐私政策 -->
        <activity
            android:name=".ui.activity.mine.account.UserPrivacyPolicyActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 关于我们 -->
        <activity
            android:name=".ui.activity.mine.setting.AboutUsActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 帮助列表 -->
        <activity
            android:name=".ui.activity.mine.setting.HelpListActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 帮助文章 -->
        <activity
            android:name=".ui.activity.mine.setting.HelpDetailsActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <!-- 钱包明细 -->
        <activity
            android:name=".ui.activity.mine.WalletDetailActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />
    </application>

</manifest>