#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME} #end

class ${NAME}Entity {

    var lists: List<DataBean>? = null

    class DataBean {
    
        var _id: Int = 0
        var _name: String? = null
    }
}
